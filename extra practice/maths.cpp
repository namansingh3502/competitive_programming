#include<iostream>
#include<cmath>
using namespace std;

int main() {
    int x,y;
    cin >>x;
    cin >>y;

    cout <<max (x,y)<<"is greater than "<<min(x,y)<<"by"<<(max(x,y)-min(x,y))<<"\n\n";
    /*It will give the bigger noand the difference between both no */

    cout<<sqrt(225)<<"\n\n";
    cout<<log(2)<<"\n\n";


    return 0;
}
